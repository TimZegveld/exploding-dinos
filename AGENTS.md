# Repository workflow

- Never push or merge changes directly to `main`.
- Make every change on a separate branch, using the `codex/` prefix for Codex-created branches.
- Deliver changes through a pull request targeting `main`.
- Only the repository owner, TimZegveld, may merge pull requests into `main`.
- Do not bypass the `main` ruleset or attempt to weaken its protection.
